document.addEventListener('DOMContentLoaded', () => {

    // const frames = ['O']

    // let position = 0;
    // function animateURL(){
    //   window.location.hash = frames[position%frames.length];
    //   position ++;
    //   setTimeout(animateURL, 200)
    // }

    // animateURL();

})